# 碳足迹追踪平台部署指南

## 部署步骤

### 1. 构建前端
在项目根目录执行：
```bash
npm run build
```

### 2. 连接到服务器
```bash
ssh root@111.231.141.121
```

### 3. 在服务器上安装必要软件
```bash
apt-get update && apt-get install -y docker.io docker-compose
```

### 4. 启动Docker服务
```bash
systemctl start docker && systemctl enable docker
```

### 5. 创建项目目录
```bash
mkdir -p /root/carbon-footprint
```

### 6. 上传文件
在本地执行以下命令：
```bash
# 上传后端文件
scp -r D:\cfp\backend root@111.231.141.121:/root/carbon-footprint/

# 上传前端构建产物
scp -r D:\cfp\dist root@111.231.141.121:/root/carbon-footprint/
```

### 7. 启动服务
在服务器上执行：
```bash
cd /root/carbon-footprint/backend && docker-compose up -d
```

### 8. 检查服务状态
```bash
docker ps
```

## 访问地址
- **前端**：http://111.231.141.121
- **后端API**：http://111.231.141.121:8080/api

## 默认账号
- **管理员**：admin / admin123
- **普通用户**：user / user123

## 注意事项
1. 确保服务器已开放80和8080端口
2. 首次启动可能需要几分钟时间初始化数据库
3. 如果遇到问题，请检查Docker容器日志

## 故障排除
- **服务无法启动**：检查Docker日志 `docker logs carbon-footprint-app`
- **数据库连接失败**：检查MySQL容器状态 `docker ps | grep mysql`
- **前端无法访问**：检查Nginx容器状态 `docker ps | grep nginx`

---

部署完成后，您的碳足迹追踪平台将在服务器上正常运行！