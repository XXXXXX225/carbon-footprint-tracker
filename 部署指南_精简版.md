# 碳足迹追踪平台部署指南（精简版）

## 项目必需文件

### 前端必需文件：
- `src/` 目录：前端源代码
- `package.json`：前端依赖配置
- `package-lock.json`：依赖版本锁定
- `vite.config.ts`：Vite构建配置
- `tsconfig.json`、`tsconfig.app.json`、`tsconfig.node.json`：TypeScript配置
- `index.html`：前端入口文件
- `.env.production`：生产环境配置

### 后端必需文件：
- `backend/` 目录：后端源代码
- `backend/pom.xml`：Maven依赖配置
- `backend/Dockerfile`：Docker构建文件
- `backend/docker-compose.yml`：Docker Compose配置
- `backend/nginx.conf`：Nginx配置

## 部署步骤

### 1. 上传必需文件到服务器
```bash
# 在本地执行，上传前端文件
scp -r D:\cfp\src D:\cfp\package.json D:\cfp\package-lock.json D:\cfp\vite.config.ts D:\cfp\tsconfig.json D:\cfp\tsconfig.app.json D:\cfp\tsconfig.node.json D:\cfp\index.html D:\cfp\.env.production root@111.231.141.121:/root/carbon-footprint/

# 上传后端文件
scp -r D:\cfp\backend root@111.231.141.121:/root/carbon-footprint/
```

### 2. 连接到服务器
```bash
ssh root@111.231.141.121
```

### 3. 安装必要的依赖
```bash
# Debian/Ubuntu系统
apt-get update && apt-get install -y docker.io docker-compose npm nodejs

# CentOS/RHEL系统
yum update -y && yum install -y docker docker-compose npm nodejs
```

### 4. 构建前端
```bash
cd /root/carbon-footprint
npm install
npm run build
```

### 5. 启动Docker服务
```bash
systemctl start docker && systemctl enable docker
```

### 6. 启动应用
```bash
cd backend
docker-compose up -d
```

### 7. 检查服务状态
```bash
docker ps
```

## 访问地址
- **前端**：http://111.231.141.121
- **后端API**：http://111.231.141.121:8080/api

## 默认账号
- **管理员**：admin / admin123
- **普通用户**：user / user123

## 注意事项
1. 确保服务器已开放80和8080端口
2. 首次启动可能需要几分钟时间初始化数据库
3. 如果遇到问题，请检查Docker容器日志

## 故障排除
- **服务无法启动**：检查Docker日志 `docker logs carbon-footprint-app`
- **数据库连接失败**：检查MySQL容器状态 `docker ps | grep mysql`
- **前端无法访问**：检查Nginx容器状态 `docker ps | grep nginx`

---

部署完成后，您的碳足迹追踪平台将在服务器上正常运行！